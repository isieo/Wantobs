require "spec_helper"

describe UserMailer do
  describe "comment_notification" do
    #let(:mail) { UserMailer.comment_notification }

    #it "renders the headers" do
    #  mail.subject.should eq("Someone commented on your post")
    #  mail.to.should eq(["to@example.org"])
    #  mail.from.should eq(["notification@wantobs.com"])
    #end

    #it "renders the body" do
    #  mail.body.encoded.should match("Hi")
    #end
  end

end
